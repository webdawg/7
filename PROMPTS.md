# Prompt Log

Every prompt submitted while working in this repo is appended here automatically
(see `.claude/settings.json` — `UserPromptSubmit` hook), preserving the design
conversation that produced the specs alongside them.
